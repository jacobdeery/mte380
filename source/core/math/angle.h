#pragma once

#include <cmath>

namespace mte {
namespace math {

double NormalizeAngle(double angle);
double DegToRad(double angle_deg);
double RadToDeg(double angle_rad);

}  // namespace math
}  // namespace mte
